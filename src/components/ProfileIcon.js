import React from "react";

const ProfileIcon = () => {
  return (
    <button type="button" className="ProfileIconButton">
      <i className="bi bi-heart"></i>
    </button>
  );
};

export default ClickableIcon;
